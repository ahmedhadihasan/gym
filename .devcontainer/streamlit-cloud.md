# Streamlit Cloud deployment

1. Open https://share.streamlit.io and deploy `ahmedhadihasan/gym`
2. Set **Main file path** to: `streamlit_app.py` (not `app.py`)
3. Save and reboot the app

`app.py` is for local Flask/PWA only. Streamlit Cloud cannot run Flask's debug server.
